name: DiffDRR
channels:
  - defaults
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - pytorch>=1.13
  - torchvision
  - pytorch-cuda=11.7
  - pydicom
  - matplotlib
  - seaborn
  - black
  - flake8
  - ipykernel
  - tqdm
  - pycuda
  - pip:
      - deepdrr
      - -e .  # Install the local package (DiffDRR)
